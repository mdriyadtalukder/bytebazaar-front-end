import { createSlice } from "@reduxjs/toolkit";
const initialState = {}
const likeProductSlice = createSlice({
    name: 'likes',
    initialState,
    reducers: {}

})
export default likeProductSlice.reducer;
export const { } = likeProductSlice.actions;