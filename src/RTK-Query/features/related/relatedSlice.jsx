import { createSlice } from "@reduxjs/toolkit";
const initialState = {}
const relatedSlice = createSlice({
    name: 'related',
    initialState,
    reducers: {}

})
export default relatedSlice.reducer;
export const { } = relatedSlice.actions;