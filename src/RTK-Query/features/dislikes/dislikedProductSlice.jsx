import { createSlice } from "@reduxjs/toolkit";
const initialState = {}
const dislikedProductSlice = createSlice({
    name: 'likes',
    initialState,
    reducers: {}

})
export default dislikedProductSlice.reducer;
export const { } = dislikedProductSlice.actions;